import styled from '@emotion/styled';

export const ListOfFilms = styled.ul`
  max-width: calc(100vw - 48px);
  margin-top: 0;
  margin-bottom: 0;
  padding: 0;
  margin-left: auto;
  margin-right: auto;
`;
